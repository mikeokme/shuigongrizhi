package com.example.shuigongrizhi.ui.theme

import androidx.compose.ui.graphics.Color

// 深色主题颜色
val DeepPurple = Color(0xFF1E1A2E) // 深紫色背景
val DarkPurple = Color(0xFF2D2640) // 暗紫色背景

// 卡片渐变色起始色
val ChampagneStart = Color(0xFF6A4A9E) // 香槟卡片起始色（紫色）
val ChampagneEnd = Color(0xFF3F2B7B) // 香槟卡片结束色（深紫色）

val SparklingStart = Color(0xFF3A7A7A) // 起泡酒卡片起始色（青绿色）
val SparklingEnd = Color(0xFF2A4A4A) // 起泡酒卡片结束色（深绿色）

val RedWineStart = Color(0xFF9A4A5A) // 红酒卡片起始色（红色）
val RedWineEnd = Color(0xFF6A2A3A) // 红酒卡片结束色（深红色）

val RoseWineStart = Color(0xFF6A4A9E) // 桃红酒卡片起始色（紫色）
val RoseWineEnd = Color(0xFF4A2A7E) // 桃红酒卡片结束色（深紫色）

val SweetWineStart = Color(0xFF3A5A7A) // 甜酒卡片起始色（蓝色）
val SweetWineEnd = Color(0xFF2A3A5A) // 甜酒卡片结束色（深蓝色）

val CognacStart = Color(0xFF7A5A4A) // 白兰地卡片起始色（棕色）
val CognacEnd = Color(0xFF5A3A2A) // 白兰地卡片结束色（深棕色）

// 应用主题色
val Primary = Color(0xFF6A4A9E) // 主色（紫色）
val Secondary = Color(0xFF3A7A7A) // 次色（青绿色）
val Tertiary = Color(0xFF9A4A5A) // 第三色（红色）

// 文字颜色
val TextWhite = Color(0xFFF5F5F5) // 白色文字
val TextGray = Color(0xFFAAAAAA) // 灰色文字

// 图标颜色
val IconLight = Color(0xFFF5F5F5) // 亮色图标
val IconSelected = Color(0xFFFF6B6B) // 选中图标（红色）